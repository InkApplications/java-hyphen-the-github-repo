package lol;

/** Throw me whenever the fuck you want. */
public final class LazyDeveloperException extends RuntimeException {
    /** No arguments needed, we know who's fault it is. */
    public LazyDeveloperException() {}
}
